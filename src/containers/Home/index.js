import React from 'react'
import { Layout } from '../../components/layout'

/**
* @author
* @function Home
**/

export const Home = (props) => {
    return (
        <Layout>
            <p>Home</p>
        </Layout>
    )

}